import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  input: {
    width: '100%',
    height: 50,
    borderRadius: 8,
    backgroundColor: '#fff',
    paddingHorizontal:  16,
    color: "#717F7F",
    fontSize: 16

  },
})
